# UE project build artifacts (compiled module binaries in Binaries/Win64 ARE
# committed on purpose: the GitHub release zip must work without Visual Studio)
Binaries/**
!Binaries/Win64/
!Binaries/Win64/UnrealEditor-Dominion.dll
!Binaries/Win64/UnrealEditor-RSDWCustomBuilds.dll
!Binaries/Win64/UnrealEditor.modules
!Binaries/Win64/RSDWCustomBuildsEditor.target
Intermediate/
Saved/
DerivedDataCache/
Build/
PakRaw/

# .NET build artifacts
obj/
bin/
publish/
*.pdb

# Per-machine configuration (created on first run — never commit personal paths)
Tools/mod.config.ini

# Runtime files the mod writes next to itself
RSDWBlankMenu/journal_*.lua
RSDWBlankMenu/journal.log

# OS noise
Thumbs.db
.DS_Store
