# UE project build artifacts
Binaries/
Intermediate/
Saved/
DerivedDataCache/
Build/
PakRaw/

# .NET build artifacts
obj/
bin/
publish/
*.pdb

# Per-machine configuration (created on first run — never commit personal paths)
Tools/mod.config.ini

# Runtime files the mod writes next to itself
RSDWBlankMenu/journal_*.lua
RSDWBlankMenu/journal.log

# OS noise
Thumbs.db
.DS_Store
